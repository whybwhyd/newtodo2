// import { createContext, useContext  } from "react";
// import uuid from 'react-uuid'
// import { useState} from "react";

// export const TodosContext = createContext(null);

// //할일 카드 내용을 담은 배열입니다. isdone은 할일 완료 여부를 나타내는 값입니다.


// return(
// <TodosContext.Provider value={{
// setCards,cards,boxTitle,setBoxTitle,boxContent,setBoxContent,boxDate,setBoxDate,
// isDone,setIsdone,handleTitle,handleContent,handleAdd,handleCbtn,handleDelete,
// handleDate,}}>
//     {children}
// </TodosContext.Provider>)
// }
// export function useTodosContext() {
//   return useContext(TodosContext);
// }